/* 3rd party imports */
import { Theme } from 'theme-ui';

export default {
	default: {
        color: 'text',
        fontFamily: 'body',
        fontSize: 2,
    },
    muted: {
        color: 'muted',
        fontFamily: 'body',
        fontSize: 2,
    },
} as Theme['text'];
/* 3rd party imports */
import { SxStyleProp } from 'theme-ui';

const root: SxStyleProp = {
	flexDirection: 'column',
	height: '100%',
};

const subroot: SxStyleProp = {
	flexDirection: 'row',
	height: '100%',
};

export default { root, subroot };